<div class="container">
    <h3>注文履歴詳細</h3>
    <div class="row">
        <div class="col-md-7">
            <table class="table table-striped">
                <tr>注文情報</tr>
                <tr><th>注文日</th><th><%= @orders.created_at.strftime("%Y/%m/%d") %></th></tr>
                <tr><td>配送先</td><td><%= @orders.postal_code %>
                <%= @orders.name %>
                <%= @orders.address %></td></tr>
                <tr><td>支払方法</td><td><%= @orders.payment_method %></td></tr>
                <tr><td>ステータス</td><td><%= @orders.status %></td></tr>
            </table>
        </div>
                <div class="col-md-3">
                    <table class="table table-striped">
                        <tr>請求情報</tr>
                        <tr><th>商品合計</th>
                            <th><%#= @orders.amount %></th>
                        </tr>
                        <tr><td>配送料</td>
                            <td><%= @orders.shipping_cost %></td>
                        </tr>
                        <tr>
                            <td>ご請求額</td>
                            <td><%= @orders.total_payment %></td>
                        </tr>
                    </table>
                </div>
    </div>
    
    
<div class="row">
    <div class="col-md-9">
        <table class="table table-striped">
            <tr>注文内容</tr>
            <tr>
                <th>商品</th>
                <th>単価</th>
                <th>個数</th>
                <th>小計</th>
            </tr>
            
            <% @order.order_details.each do |t| %>
            <tr>
                <td><%= t.order.name %></td>
                <td><%= t.order.total_payment %></td>
                <td><%#= t.order.amount %></td>
                <td><%#= t.total_payment * t.amount %></td>
            </tr>
            <% end %>
        </table>
    </div>
</div>
</div>
