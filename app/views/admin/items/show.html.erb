<h1>商品詳細</h1>

<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>

  <%=attachment_image_tag @item,:image,:fill, 60, 60, fallback: "no_image.jpg"%></div>

<div class='col-md-8 offset-md-1'>
<table class="table">
  <tbody>
    <tr>
     <th>商品名</th>
     <th><%= @item.name %></th>
    <tr>
  　 <th>商品説明</th>
     <th><%= @item.introduction %></th>
    </tr>
    <tr>
      <th>ジャンル</th>
      <th><%= @item.genre.name %></th>
    </tr>
    <tr>
     <th>税込価格
     <p>（税別価格）</p>
     </th>
     <th>¥<%= convert_to_taxed(@item.price).to_s(:delimited, delimiter: ',') %><br>
     (¥<%= @item.price.to_s(:delimited, delimiter: ',') %>)</th>
    </tr>
     <tr>
     <th>販売ステータス</th>
     <th><% if @item.is_active %>販売中<% else %>販売中止<% end %>
　　 </th>
    </tr>
  </tbody>
</table>
<%= link_to "Edit", edit_admin_item_path(@item) ,class:"btn btn-primary"%>
</div>

</div>
</div>