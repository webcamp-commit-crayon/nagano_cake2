<div class="container">
  <h1>注文履歴詳細</h1>

      <div class="cartitem-area clearfix border-bottom border-dark p-3">


          <table class="table table-hover table-inverse">
            <tr>
              <td>購入者</td>
              <td><%=link_to admin_customer_path(@order.customer) do %>
              <%= @order.customer.last_name %><%= @order.customer.first_name %>
              <% end %></td>
            </tr>
            <tr>
              <td>注文日</td>
              <td><%= @order.created_at %></td>
            </tr>
            <tr>
              <td>配送先</td>
              <td><%= @order.postal_code %><%= @order.address %><%= @order.name %></td>
            </tr>
            <tr>
              <td>支払い方法</td>
              <td><%= @order.payment_method %></td>
            </tr>
            <tr>
              <td>注文ステータス</td>
              <td>
              <%#= form_with(url: admin_order_detail_path, method: :patch, local: true) do |f| %>
              <%=select_tag "status", options_for_select([["入金待ち",0],["入金確認中",1],["製作中",2],["発送準備中",3],["発送済",4]])%>
              </td>
            </tr>
          </table>
      </div>

<div class="row">

 <div class="col-sm-9">
<table class="table table-hover table-inverse">
  <tr>
    <th>商品名</th>
    <th>単価（税込）</th>
    <th>数量</th>
    <th>小計</th>
    <th>制作ステータス</th>
    <th></th>
  </tr>
  <tr>
    　<%# @order.order_details.each do |detail| %>
　   <%# @name = detail.item_id.name %>
  　<%# end %>
    <td><%#= @name %></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
  </tr>
</table>
</div>

<div class="col-sm-3">
  <table>
    <tr>
      <td>商品合計</td>
      <td></td>
    </tr>
    <tr>
      <td>送料</td>
      <td></td>
    </tr>
    <tr>
      <td>請求合計額</td>
      <td></td>
    </tr>
  </table>
</div>

</div>
</div>